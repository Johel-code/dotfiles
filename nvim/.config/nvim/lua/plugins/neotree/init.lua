return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    filesystem = {
      filtered_items = {
        hide_dotfiles = false, -- Set to true to hide dotfiles
        visible = true, -- Optional, sets the initial visibility of filtered items
      },
    },
  },
}
